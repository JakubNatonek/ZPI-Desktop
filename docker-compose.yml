include:
  - ./ZPI-Desktop-Backend/docker-compose.yml

services:

  frontend:
    build:
      context: ./ZPI-Desktop-App
      dockerfile: Dockerfile
    container_name: zpi-desktop-frontend
    ports:
      - "4200:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  postgres_data:
